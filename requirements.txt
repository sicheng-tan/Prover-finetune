# NOTE:
# vLLM (and its CUDA-matched torch stack) should be installed by user beforehand.
# To reduce compatibility conflicts, do not pin vLLM-managed core deps here
# (e.g. torch/transformers/huggingface_hub/vllm).
accelerate>=0.27.0
datasets>=2.14.0
regex>=2023.10.3
openai>=1.0.0
lean-interact>=0.9.0
hf-transfer
peft>=0.10.0
bitsandbytes>=0.43.0
trl>=0.9.0

# FLRQ/run_quantize.py: tqdm (progress); numpy (calib + r1_sketch); safetensors (imports).
# torch / transformers still follow the NOTE above (vLLM or finetune stack).
tqdm>=4.65.0
numpy>=1.24.0
