experiment:
  name: minif2f-deepseek-prover-v2-7b-pass16
  split: test
  max_samples: null
  pass_k: 16
  gpu_ids: [0]
  verbose_logging: true
  output_dir: outputs/experiments/minif2f-deepseek-prover-v2-7b-pass16

model:
  name_or_path: deepseek-ai/DeepSeek-Prover-V2-7B
  model_type: deepseek_prover_v2
  adapter_path: null
  require_gpu: true
  use_vllm: true
  inference_timeout_sec: null
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.95
  do_sample: true
  tensor_parallel_size: null  # null => use len(experiment.gpu_ids)
  gpu_memory_utilization: 0.85
  vllm_batch_size: 16
  lean_parallel_workers: null  # null => min(vllm_batch_size, cpu_count)
  dump_generated_lean_debug: true

minif2f:
  source_type: local_json
  json_path: data/processed/test.json
  jsonl_path: data/processed/test.jsonl
  root_dir: data/
  split_file: data/processed/test.txt

lean:
  project_config_path: configs/lean_project.example.yaml
